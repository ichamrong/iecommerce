/**
 * Domain events emitted by the audit bounded context (e.g. AuditRecordedEvent for downstream
 * consumers). Optional; used when event-driven integration is required.
 */
package com.chamrong.iecommerce.audit.domain.event;
